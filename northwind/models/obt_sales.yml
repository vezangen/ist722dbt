version: 2

models:
  - name: obt_sales
    description: The sales fact table combined with all of its outrigger dimensions into one big table. Transaction fact table with one row per product including product info.
